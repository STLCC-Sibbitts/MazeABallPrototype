using UnityEngine;
using System.Collections;

public class DEBUG_Rotate90 : MonoBehaviour {

	// Use this for initialization
    void Start()
    {
        transform.Rotate(270.0f, 0.0f, 0.0f);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
